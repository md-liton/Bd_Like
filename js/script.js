$(function() {
    // slider
    $('.main_div').slick({
        autoplay :true,
        autoplaySpeed : 3000,
        arrows : false,
      });
      // count_up
      $('.count_up').counterUp({
        delay: 10,
        time: 1000
      });
    // father icon js start
    feather.replace()
    // father icon js end
});

